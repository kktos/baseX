{
  "name": "basex",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "parse": "node ./dist/basex.js parse --vars --arrays --strings --disasm ./examples/test_chr.bas",
    "run": "node ./dist/basex.js run --list ./examples/test_chr.bas",
    "build": "esbuild src/main.ts --bundle --platform=node --outfile=./dist/basex.js --sourcemap --legal-comments=none --minify --target=node18 --external:terminal-kit",
    "format": "rome format .",
    "lint": "rome check .",
    "format:fix": "rome format --write .",
    "lint:fix": "rome check --apply-suggested ."
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "@types/terminal-kit": "^2.5.1",
    "@types/yargs": "^17.0.20",
    "esbuild": "^0.17.5",
    "rome": "^11.0.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "terminal-kit": "^3.0.0",
    "yargs": "^17.6.2"
  }
}
