{
  "name": "basex",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "node ./dist/basex.js parse --vars --arrays --strings --disasm ./examples/test_let.bas",
    "build": "esbuild src/main.ts --bundle --platform=node --outfile=./dist/basex.js --sourcemap --legal-comments=none --minify --target=node18",
    "format": "rome format .",
    "format:fix": "rome format --write .",
    "lint": "rome check .",
    "lint:fix": "rome check --apply-suggested ."
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "@types/yargs": "^17.0.20",
    "esbuild": "^0.17.5",
    "rome": "^11.0.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "yargs": "^17.6.2"
  }
}
